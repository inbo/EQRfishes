% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine_zonation.R
\name{determine_zonation}
\alias{determine_zonation}
\title{determine the index typology for records of a dataset}
\usage{
determine_zonation(dataset, version = c("new", "old"))
}
\arguments{
\item{dataset}{dataset on location measures with at least columns
\itemize{
\item \code{width_river} width of the river at the sample location (meters),
\item \code{slope} slope of the river at the sample location (per thousand),
\item \code{tidal} tidal effect present (TRUE) or absent (FALSE)?
\item \code{index_type_code} type based on salinity of the sample location:
\itemize{
\item \code{"ZTWA"} for freshwater,
\item \code{"YZRP"} for estuarine Ijzer,
\item \code{"SCHM"} for estuarine Schelde mesohaline,
\item \code{"SCHO"} for estuarine Schelde oligohaline,
\item \code{"SCHZ"} for estuarine Schelde freshwater,
\item \code{"ZTWZ"} for estuarine tributaries with freshwater
}
}}

\item{version}{'new' version with bron and brabeel or 'old' version without
these two indextypologies?
Defaults to 'new'.
This information will be used if dataset has no column \code{version} or to
replace NA values in column \code{version}.}
}
\value{
input dataset with additional column \code{indextypology}
}
\description{
This function determines the index typology based on the given
dataset with measurements.
Recently 2 new index typologies were added: 'bron' (replacing 'upstream' in
rivers up to a width of 2 m) and 'brabeel' (replacing 'brasem' and 'barbeel'
in rivers up to a width of 30 m), and both versions are available here:
\itemize{
\item \strong{new} (default) uses the new version with 'bron' and
'brabeel' included,
\item \strong{old} uses the version before 'bron' and 'brabeel' were
added
}
This version can be indicated on the level of the individual records in the
dataset as an additional column or on the level of the whole dataset by using
argument \code{version}.
When both are added, NA values of column version in the dataset will be
replaced by the version given in argument \code{version}.
}
\seealso{
Other main:
\code{\link[=calculate_eqr]{calculate_eqr()}}
}
\concept{main}
