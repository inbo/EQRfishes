% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine_indextypology.R
\name{determine_indextypology}
\alias{determine_indextypology}
\title{determine the index typology for records of a dataset}
\usage{
determine_indextypology(dataset, version = c("new", "old"))
}
\arguments{
\item{dataset}{dataset on location measures with at least columns
\itemize{
\item \code{width_river} width of the river at the sample location (meters),
\item \code{slope} slope of the river at the sample location (per thousand),
\item \code{index_type_code} type based on salinity of the sample location
(detailed description in
\code{vignette("indextypologies", package = "EQRfishes")}):
\itemize{
\item \code{"ZTWA"} for freshwater that is not under tidal influence,
\item \code{"YZRP"} for the tidal section of the IJzer estuary (Belgium),
\item \code{"SCHM"} for the mesohaline zone (salinity 5-18) of the Zeeschelde
estuary between Zandvliet (Dutch-Belgian border) and Antwerpen (Belgium),
\item \code{"SCHO"} for the oligohaline zone (salinity 0.5-5) of the Zeeschelde
estuary (Belgium) between Antwerpen en Rupelmonde including the Rupel
river,
\item \code{"SCHZ"} for the freshwater zone of the Zeeschelde estuary (Belgium)
between Rupelmonde and Gent including the River Durme
(limnetic zone: < 0.5),
\item \code{"ZTWZ"} for freshwater tributaries of the Zeeschelde estuary (Belgium)
that are under tidal influence
}
}}

\item{version}{'new' version with fish-based river source IBI and
fish-based lowland IBI or 'old' version without these two index typologies?
Defaults to 'new'.
This information will be used if \code{dataset} has no column \code{version} or to
replace NA values in column \code{version}.}
}
\value{
input dataset with additional columns \code{indextypology},
\code{indextypology_short} and \code{indextypology_dutch}.
(Any of these columns can be used in \code{calculate_eqr()}, but the column name
must be (changed to) \code{indextypology})
}
\description{
This function determines the index typology based on the given
dataset with measurements.
Recently 2 new index typologies were added: 'fish-based river source IBI'
(replacing 'fish-based upstream IBI' in rivers up to a width of 2 m) and
'fish-based lowland IBI' (replacing 'fish-based bream IBI' and
'fish-based barbel IBI' in rivers up to a width of 30 m),
and both versions are available here:
\itemize{
\item \strong{new} (default) uses the new version with
'fish-based river source IBI' and 'fish-based lowland IBI' included,
\item \strong{old} uses the version before 'fish-based river source IBI'
and 'fish-based lowland IBI' were added
}
This version can be indicated on the level of the individual records in the
dataset as an additional column or on the level of the whole dataset by using
argument \code{version}.
When both are added, NA values of column version in the dataset will be
replaced by the version given in argument \code{version}.
}
\seealso{
Other main:
\code{\link[=calculate_eqr]{calculate_eqr()}}
}
\concept{main}
